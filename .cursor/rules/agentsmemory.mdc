---
description: Local palace wake — Enter checkout (promptanatomy.cloud)
alwaysApply: true
---

# agentsmemory — .cloud checkout

This folder is the Prompt Anatomy **Enter** lesson (`promptanatomy.cloud`, repo `DITreneris/lead`). Memory is the local palace at `http://127.0.0.1:8080/mcp` (user-level MCP). Cursor has no Stop hook — this rule is the write half.

Default wing: `promptanatomy`. **Do not mint a `cloud` or `lead` wing.** MCP `default_wing` is empty — every `am_*` call must pass `wing: promptanatomy`. `wing: "*"` only for a deliberate cross-project question.

This is not the Hub and not Use. Use is `DITreneris/automation` (`promptanatomy.info`).

## First actions of every session

1. `am_status` — `mode` must be `local`, workspace slug `local`. A different workspace is a stop — write nothing.
2. `am_skillset` and follow it.
3. `am_load_skill("promptanatomy")`.
4. `am_bootstrap` with `wing: promptanatomy`.

## Recall

Enter-spoke role, 5-part public UI vs six-block training, chrome only `.app` + `.pro` + `.site`, entity footer QW1b, hub Hero has no outbound `.cloud`, two-channel pricing (39/99 vs 399) → `am_search` wing `promptanatomy`. Checkout and access live on `.app`, not here.

## Persist

`am_diary_write` agent_name `cursor-memory`, wing `promptanatomy` only when this session settled a **new** ecosystem fact. Do not re-file hub deploy/access canon.

## Never mine into the palace

This spoke's source, `libraryPrompts`, DS docs, CHANGELOG, PDF MD, sibling spoke repos. Do not dump the lesson tree into drawers. Code stays here; palace stays memory.

No signup, Stripe, or login unless explicitly requested.
